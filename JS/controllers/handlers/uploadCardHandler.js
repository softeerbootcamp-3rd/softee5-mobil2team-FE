import { showDialog } from "./modalHandler";

const modal = document.querySelector(".modal");

export const uploadCardHandler = (target) => {
  showDialog();
  //   modal.addEventListener("submit", onAddSubmit);
};
